<?xml version="1.0" encoding="UTF-8"?>
<!--
  easyexcel-test：fork 集成测试模块。

  - maven.deploy.skip=true：不发布到私有仓库（仅本地回归用）
  - surefire include：只跑 /com/alibaba/easyexcel/test/core/ 下的回归用例，
    排除 easyexcel-markdown 模块的 76 个测试（它们由各自模块 surefire 跑）
  - 211 个集成测试覆盖 easyexcel-core 写入 / 读取 / 异常 / 字符集 / 模板等

  见根 pom 注释了解 fork 整体结构。
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.easy4j</groupId>
        <artifactId>easyexcel-parent</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <url>https://github.com/easy-4-java/${project.artifactId}</url>
    <packaging>jar</packaging>
    <artifactId>easyexcel-test</artifactId>
    <name>easyexcel-test</name>

    <properties>
        <!-- 集成测试模块不发布到私有仓库，仅本地 + CI 跑 -->
        <maven.deploy.skip>true</maven.deploy.skip>
    </properties>

    <dependencies>
        <!-- ===== Fork 自家核心 ===== -->
        <dependency>
            <groupId>io.github.easy4j</groupId>
            <artifactId>easyexcel-core</artifactId>
        </dependency>

        <!-- ===== JSON / Spring Boot 集成测试（版本由根 pom dependencyManagement 统一锁定） ===== -->
        <dependency>
            <groupId>com.alibaba.fastjson2</groupId>
            <artifactId>fastjson2</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
        </dependency>

        <!-- ===== 日志（slf4j 桥 + logback 后端；版本由根 pom 统一锁定） ===== -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>jcl-over-slf4j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>log4j-over-slf4j</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <includes>
                        <include>/com/alibaba/easyexcel/test/core/**/*.java</include>
                    </includes>
                    <testFailureIgnore>false</testFailureIgnore>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
